# Insight Feeder — .env.example
# Copy to .env and fill values. Edge Functions (Deno) read with Deno.env.get().

# Supabase
SUPABASE_URL=https://YOUR-PROJECT.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Auth token to protect cron -> Edge Function triggers (optional)
EDGE_TOKEN=change-me-very-long-random

# Feeds & fetching
FEED_USER_AGENT="InsightFeeder/1.0 (+contact@example.com)"
FETCH_TIMEOUT_MS=15000
MAX_ITEMS_PER_RUN=500

# LLM / AI enrichment
LLM_PROVIDER=openai              # or anthropic, azure-openai, etc.
LLM_API_KEY=sk-your-key
LLM_MODEL=gpt-4o-mini            # pick your deployed model name
LLM_MAX_TOKENS=800

# Optional: image generation (if you ever use AI illustrations)
IMG_PROVIDER=disabled            # or openai, replicate, etc.
IMG_API_KEY=

# News APIs (optional connectors). Leave blank if unused.
NEWSDATA_API_KEY=
NEWSCATCHER_API_KEY=
EVENTREGISTRY_API_KEY=
AYLIEN_API_KEY=
AYLIEN_APP_ID=
GDELT_ENABLED=false

# Storage (if you mirror thumbnails legally)
STORAGE_BUCKET=news-media
STORAGE_PUBLIC_URL=https://YOUR-PROJECT.supabase.co/storage/v1/object/public

# Feature flags
ENABLE_HTML_EXTRACTION=false     # only set true if robots/terms allow
ENABLE_VECTOR_EMBEDDINGS=false   # set true after you install pgvector

# Runtime
NODE_OPTIONS=--max-old-space-size=2048
LOG_LEVEL=info                   # debug|info|warn|error
